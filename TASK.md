# 完整首轮任务

有几份独立产生的有限状态规则，需要比较它们接受的token序列是否相同。请写DFA语言运算库，不解析正则表达式。

每台DFA最多80状态，有显式排序字母表（最多8个不同字符串token）、初态、接受态集合及可能不完整的转移表。缺失转移视为进入隐式拒绝汇点；所有运算先统一为双方字母表并集的字典序，原机未声明的token也进入汇点。状态id只能是整数，未知状态引用、重复字母和重复转移拒绝。

实现交、并、差、补和最小化。最小化只保留可达状态，用分割细化等算法产生真正最小的完整DFA；输出从初态0开始按token顺序BFS重新编号。补相对于显式字母表的全部有限串，空字母表下只有空串。提供原可达状态到最小化状态映射，隐式汇点若可达也要标明。

等价/包含判断若失败返回最短见证token列表，同长度按token序列字典序最小；空列表是合法见证，不能和不存在见证混淆。检查该见证在双方上的接受性，不以固定长度枚举代替产品图搜索。

小机穷举短串交叉核验语言运算，并用区分后缀检查最小化后任意两状态确实可区分。覆盖全接受/全拒绝、不可达状态、缺转移、不同字母表和空串反例。演示两台不等价机器及可复核的最短见证。

使用 Python 3.14.7 标准库，在 Windows 原生环境运行。范围就是这个库，不加前后端、网络服务、外部数据库或Docker。写好必要测试和简短README，说明接口、输入限制及运行命令。

用 `python demo.py` 在约8秒内演示一个正常结果和一个实际触发的失败；用 `python -m unittest discover -s tests -v` 跑测试。独立参考只用少量固定种子的小样本，不做长时间压力测试。先把核心实现、边界和必要验证一次完成，不扩展任务，也不用重复跑同一批检查。

只在本轮完成，不向我追问或要求回复“继续”。代码只写当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不启用Actions、CI、部署或Dependabot，不向远端推送。演示输出必须由代码实际计算；最后如实说明通过、失败和未执行的验证。
