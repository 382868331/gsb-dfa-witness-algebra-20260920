# 有限自动机的语言代数与最短见证

这是0-1任务的初始骨架，核心功能尚未实现。完整任务见TASK.md。

环境：Python 3.14.7 标准库；Windows；无第三方依赖，无需安装其他包。

演示：`python demo.py`
测试：`python -m unittest discover -s tests -v`

初始demo只显示待实现提示，不是业务演示。初始检查仅验证运行时与骨架可执行，不代表功能通过。
